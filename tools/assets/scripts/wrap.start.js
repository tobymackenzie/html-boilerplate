(function(window){
	//--localize window to allow for minification
	var global = window;

	//--localize some global variables to allow for minification
	var ENV;
	var define = global.define;
	var require = global.require;
